-- TODO Finish schema!

CREATE TABLE playbyplay (
    GameID varchar(32),
    PlayID integer,
    Season varchar(16),
    TimeRemaining varchar(16),
    TeamID varchar(16),
    PlayerNameID varchar(32),
    Event varchar(16),
    EventDetail varchar(64)
)
;

